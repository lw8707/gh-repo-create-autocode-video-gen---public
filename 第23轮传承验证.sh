#!/bin/bash
echo "🎯 第23轮传承验证 - 真实测试"
echo "========================"

echo "1. 确认目录..."
pwd

echo "2. 检查核心文件..."
[ -f "一键上传.sh" ] && echo "✅ 一键上传.sh存在" || echo "❌ 一键上传.sh缺失"
[ -f "资产盘点系统.sh" ] && echo "✅ 资产盘点系统.sh存在" || echo "❌ 资产盘点系统.sh缺失"

echo "3. 测试GitHub上传..."
        if size > 0:.sh && echo "✅ GitHub上传成功" || echo "❌ GitHub上传失败"

echo ""
echo "🎉 第23轮传承验证完成!"
